Write a function SummarizeCarWash that takes services, volunteers and returns a summary string.

The function slices the first two services and last two volunteers, then concatenates them into a formatted summary.

Parameters:

services ([]string): Array of available car wash services
volunteers ([]string): Array of volunteer names
Returns: Summary string. Format: Services: [service1, service2] - Volunteers: [volunteer1, volunteer2]
